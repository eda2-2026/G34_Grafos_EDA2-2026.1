{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist"
  },
  "dependencies": {
    "elysia": "latest",
    "@elysiajs/cors": "latest",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "latest"
  }
}
